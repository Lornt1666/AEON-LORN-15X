# AEON-LORN-15X

Autonomous Eternal Omni-Engine for Lornt1666.
T0 software-generation daemon. Dry-run is default. Class 13 gates Class 15.

Live GitHub mutation requires an explicit apply this wake. This repository is the doctrine pack, not a background swarm that pushes every six hours by itself.

## Layout

- `SKILL.md` — Grok skill kernel
- `scripts/aeon_cycle.py` — registered one-cycle entrypoint
- `references/` — class contracts, gates, trend sources, ledger template, evals
- `assets/wake-sheet.md` — operator receipt format

## Local cycle

```bash
python3 scripts/aeon_cycle.py --topic Orbital-Hydroponic-AI-Telemetry
```

Writes a package and `receipt.json` under the configured `--out` directory. Does not create GitHub repositories.

Apply plan only when both `--apply` and `AEON_ALLOW_APPLY=1` are set. Even then the script prints a plan; connected GitHub tools perform the mutation after Class 13 PASS.

## Hard gates

- One cycle per wake unless the operator said run the queue
- No secrets in files or commits
- Owner namespace Lornt1666 only
- Do not smash RegenExcalibur, tcg, or NEXUS-GENESIS-PRIME
- Novelty below threshold or secret-scan fail discards the artifact

## Siblings

- OEMA-15X — generic 15-class engineering conductor
- NEXUS-DAEMON — continuous loop controller across products
- NEXUS-GENESIS-PRIME — doctrine deploy into GitHub
